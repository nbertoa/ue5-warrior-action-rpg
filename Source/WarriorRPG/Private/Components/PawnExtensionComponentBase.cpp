// PawnExtensionComponentBase.cpp
// Implementation for UPawnExtensionComponentBase.
// Intentionally minimal — all helpers are template functions defined in the header
// because the compiler needs the full definition at each call site.

#include "Components/PawnExtensionComponentBase.h"
