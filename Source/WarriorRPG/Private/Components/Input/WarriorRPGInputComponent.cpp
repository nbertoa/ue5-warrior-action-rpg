// WarriorRPGInputComponent.cpp
// Implementation file for UWarriorRPGInputComponent.
// Intentionally minimal — the core logic is in the template function defined in the header,
// which is required because the compiler needs the full template definition at each call site.

#include "Components/Input/WarriorRPGInputComponent.h"
